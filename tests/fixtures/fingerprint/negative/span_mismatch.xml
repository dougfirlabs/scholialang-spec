<!--
  NEGATIVE — span_mismatch  (fails at the CONSUMER re-verification layer)

  The fingerprint is well-formed (notation-valid), but `location` points at a
  span that does NOT hold the fingerprinted symbol. Against sources/foo_v1.py,
  lines 13:14 are bar(), while the fingerprint is foo()'s. The span and the
  fingerprint disagree at authoring time.

  This is NOT a notation failure — `fingerprint_well_formed` passes (well-formed
  value + location present). A consumer that recomputes over the span at
  `location` and compares to the declared fingerprint detects the mismatch.

  Expected verdict `span_mismatch` — see manifest.yaml.
-->
<Step id="s1" name="location span disagrees with fingerprint">
  <Goal id="G_01" scope="trace" priority="required">Fingerprint foo() but point location at the wrong span.</Goal>
  <Observation id="Obs_01" location="src/example/foo.py:13:14" fingerprint="sha256:8f4a9d2c1b3e">Claims foo() but the span is bar().</Observation>
  <Finding id="F_01" status="unmet">location span does not hold the fingerprinted symbol.</Finding>
  <Concluding id="C_01" for_goal="G_01" status="unmet">REFER:Obs_01 AND REFER:F_01 — span and fingerprint disagree.</Concluding>
</Step>
