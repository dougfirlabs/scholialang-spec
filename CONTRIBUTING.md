# Contributing

Scholia language changes should include:

- a spec update
- at least one fixture trace when behavior changes
- a coordinated implementation update in `scholialang`

For larger changes, open an RFC using `GOVERNANCE.md`.

